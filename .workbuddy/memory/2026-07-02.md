# 2026-07-02 工作记录

## 家庭点菜小程序 PRD + UI 设计稿

- 已创建完整 PRD 文档：`deliverables/software-company/family-order-prd.md`
- 已使用 ardot-design-assistant 在画布上产出两个核心页面：
  - 菜单页：搜索、分类 Tab、卡片式菜品网格、底部五栏导航
  - 点餐页：页面标题、今日点餐/历史记录 Tab、带加号按钮的菜品卡片、购物车摘要条
- 视觉风格：暖橙奶油色系、米白背景、白色卡片、16px 圆角、软阴影，适配老人小孩操作

## 后续可继续项

- 补充剩余页面：采购页、统计页、我的页（UI 设计稿已有5页，canvas: https://ardot.tencent.com/file/699465594818587）
- P1 增强功能：评分/收藏/一周食谱/数据备份（T19-T22）
- H5 网页端开发（Vue3 + Vant，P2）
- 部署到微信云开发环境并真机调试

---

## 家庭点菜小程序 — 架构设计 + 代码实现 + QA 验证（完整 SOP）

- **架构设计文档**：`deliverables/software-company/family-order-architecture.md`
  - 分层架构：Pages → Services → CloudAdapter → CloudDB
  - 事件总线 EventBus 解耦跨页面通信
  - 双通道策略：联网走云端，离线走缓存
  - 25个任务 T01-T25，P0核心18个 + P1增强4个 + P2测试3个
- **代码实现**：97个源文件，微信小程序原生开发 + 云开发
  - 项目路径：`family_order/miniprogram/` + `family_order/cloudfunctions/`
  - 15个页面、16个组件、12个服务、6个云函数、9个工具函数
  - 金额以分存储、CSS变量体系、忌口检测、离线队列
- **QA 验证报告**：`deliverables/software-company/family-order-qa-report.md`
  - 第1轮：12个问题（1严重+4中等+7轻微），路由到 Engineer
  - 第2轮回归：5个Bug全部修复确认，ALL_PASS，路由 NoOne
- **已修复的 Bug**：
  - 🔴 Dish 模型缺 totalCost → dish-service 中附加 calcDishCost 计算值
  - 🟡 cloud-adapter _sanitizeArrays → 白名单数组字段 null→[]，其余 delete
  - 🟡 menu.js 缺 onAddDish → 新增方法导航到 dish-edit
  - 🟡 purchase.js 缺 _originalIndex → forEach 附加索引
  - 🟡 join-family 云函数缺防御检查 → 添加 memberRes.data.length 检查

## 部署准备 — 云函数 package.json + 上线指南

- 为 6 个云函数全部补了 package.json（wx-server-sdk ~2.6.3）
- 确认项目现状：AppID 占位、云环境 ID 占位、TabBar 图标缺失
- 输出完整上线指南覆盖：注册 AppID → 导入项目 → 开通云开发 → 安装云函数依赖并上传 → 创建数据库集合 → 处理 TabBar 图标 → 真机调试

## 部署落地 — 图标 + AppID 修正

- 已生成 10 张 TabBar 占位图标（5 个标签 × 普通/选中态），81×81 PNG，位于 `family_order/miniprogram/assets/icons/`
- 已将 `project.config.json` 的 AppID 从占位值改为真实小程序 AppID `wx55d032bf719fa612`
- 待办：开通云开发环境、上传云函数、创建 9 个数据库集合

## 导入调试修复

- 修复 6 个 service 中 `getApp().cache` 在懒加载下为 undefined 的崩溃：`getApp().cache` → `(getApp() || {}).cache`
- 修复 `app.wxss` 中在微信小程序不支持的「规则块内 @import」语法，将 `theme-dark.wxss` 提到顶层引入，并把其选择器改为 `page[data-theme="dark"]`
- 已扫描全部 .wxss：无花括号不匹配、无规则内 @import

## 界面渲染修复 + 补齐静态资源

- 修复 `styles/global.wxss`：删除微信小程序不支持的 `*` 通配符、`textarea` 元素选择器、`::placeholder` 伪元素
- 修复 `pages/login/login.wxss`：删除 `.form-input:focus` 不支持伪类
- 修复 `pages/inventory/inventory.wxml`：将模板字符串 `${}` 改为 WXML 支持的字符串拼接
- 系统扫描确认：全部 WXML 无模板字符串、全部 WXSS 无通配符/非法伪元素/规则内 @import
- 补齐全部缺失图片资源：
  - `assets/icons/logo.png`
  - `assets/icons/dish-placeholder.png`
  - `assets/avatars/default.png`
  - `assets/avatars/dad.png/mom.png/child.png/elder.png`
  - `assets/avatars/other-1.png` ~ `other-8.png`
- 已将 `cloud.init.js` 的 `CLOUD_ENV_ID` 改为真实环境 ID `cloud1-d1g6p5n175599282e`
- 已将 `project.config.json` 从 `miniprogram/` 移到 `family_order/` 根目录，添加 `miniprogramRoot` 和 `cloudfunctionRoot` 配置，改为标准云开发结构
- 用户实际运行路径为 `D:\Code\Family Order\family_order\`，需同步上述改动
- 待办：用户重新从 `family_order` 父目录导入 → 创建 9 个数据库集合 → 上传 6 个云函数
