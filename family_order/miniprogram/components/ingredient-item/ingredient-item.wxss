/** ingredient-item.wxss */
@import '../../styles/mixins.wxss';

.ingredient-item {
  padding: var(--spacing-xs) var(--spacing-sm);
  background-color: var(--color-surface);
  border-bottom: 1rpx solid var(--color-divider);
}

.ingredient-item--used-up {
  opacity: 0.5;
}

.ingredient-item__check {
  width: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.check-icon {
  font-size: 20px;
  color: var(--color-primary);
}

.ingredient-item__info {
  min-width: 0;
  gap: 4rpx;
}

.ingredient-item__name-row {
  align-items: center;
  gap: 8px;
}

.ingredient-item__name {
  font-size: var(--font-size-body);
  font-weight: var(--font-weight-medium);
  color: var(--color-on-surface);
}

.used-up-tag {
  font-size: 10px;
  color: #fff;
  background-color: var(--color-error);
  border-radius: 4px;
  padding: 1px 6px;
  flex-shrink: 0;
}

.ingredient-item__meta {
  gap: var(--spacing-xs);
}

.ingredient-item__unit, .ingredient-item__stock {
  font-size: var(--font-size-caption);
  color: var(--color-on-surface-muted);
}

.ingredient-item__price {
  font-size: var(--font-size-caption);
  color: var(--color-primary);
}

.ingredient-item__expiry {
  font-size: var(--font-size-caption);
}

.ingredient-item__actions {
  flex-shrink: 0;
  gap: 8px;
}

.ingredient-item__action {
  padding: 4px 0;
}

.action-text {
  font-size: var(--font-size-caption);
  color: var(--color-primary);
}

.action-text--danger {
  font-size: var(--font-size-caption);
  color: var(--color-error);
}

.used-up-btn {
  padding: 4px 0;
}
